-- ============================================================
-- Миграция: Добавление поля time_spent в таблицу tasks
-- Описание: Поле для хранения затраченного времени (в часах)
-- Требования: 1.2, 4.2
-- ============================================================

ALTER TABLE `tasks`
ADD COLUMN `time_spent` DECIMAL(6,1) NULL DEFAULT NULL
AFTER `closed_at`;
