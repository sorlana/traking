# Запрет любого HTTP-доступа к этой папке (второй эшелон защиты)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
